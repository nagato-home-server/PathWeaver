イベント駆動型オープンネットワーク制御基盤

現時点における構想・設計・仕様整理

1. プロジェクトの目的

本プロジェクトは、StrongSwan、VPP、FRRoutingなどの既存ネットワークOSSを統合し、ネットワークの構成や経路を宣言的かつイベント駆動で制御する、オープンなネットワーク制御プレーンを構築することを目的とする。

本プロジェクトは、新しいVPN方式や独自の暗号化プロトコルを開発するものではない。また、既存の商用VPNルータやSD-WAN製品を、そのまま低価格なOSS製品で置き換えることを主目的としない。

標準IPsecや既存のルーティング・転送機能をデータプレーンとして利用し、その上位に次の機能を持つ制御プレーンを構築する。

宣言的なIntentの受付

複数OSSの統一的な状態管理

イベントに基づく状態更新

経路候補の選択

安全な経路遷移

障害時のロールバック

経路選択理由と状態遷移の記録

新しい経路制御アルゴリズムを実験できる拡張基盤

SD-WAN的な拠点間経路制御は、本制御基盤の有効性を示す最初の実証ユースケースとして扱う。

2. 本プロジェクトの位置付け

本プロジェクトは、単純な「OSS版SD-WAN」ではない。

商用SD-WAN製品では、VPN、ルーティング、品質測定、ポリシー、フェイルオーバーなどが一体化されている。一方で、その内部構造や制御ロジックはベンダー固有であり、新しいアルゴリズムを追加したり、状態遷移を変更したりすることは難しい。

本プロジェクトでは、既存OSSを個別の機能部品として扱い、それらを一つの状態モデルと制御プレーンで統合する。

概念構成は次のとおりである。

利用者・上位システム
        │
        ▼
      Intent
        │
        ▼
   Policy Engine
        │
        ▼
Path Selection Engine
        │
        ▼
Transition Engine
        │
        ▼
State Synchronization Engine
        │
        ├─────────────┬─────────────┐
        ▼             ▼             ▼
StrongSwan Adapter   VPP Adapter   FRR Adapter
        │             │             │
        ▼             ▼             ▼
     IPsec SA       Forwarding     Routing

ここで重要なのは、制御プレーンが単に各OSSのAPIを順番に呼ぶだけではないことである。

制御プレーンは、利用者が要求する目標状態と、実際に観測された状態との差分を継続的に監視し、システム全体を目標状態へ収束させる。

3. 想定利用者

3.1 初期の利用者

本プロジェクトの初期ターゲットは、一般企業の完成品導入ではなく、研究・実証・開発用途を中心とする。

想定する利用者は次のとおりである。

大学や研究機関

ネットワーク研究者

OSS開発者

通信事業者の研究開発部門

SIerの技術検証部門

独自ネットワーク製品を開発する技術者

ネットワーク制御の教育環境

これらの利用者に対し、新しい経路選択方式、障害復旧方式、状態同期方式、ポリシー制御方式などを試せる共通基盤を提供する。

3.2 将来の利用者

研究成果が成熟した場合、次の利用も想定できる。

小規模ネットワークでのPoC

学校や自治体での技術検証

中小企業での限定的な実証運用

OSSネットワークアプライアンスへの組み込み

通信事業者やクラウド事業者の制御基盤への応用

Zero Trust、SASE、Edge制御への展開

ただし、商用VPNルータの置き換えを初期目標にはしない。

4. 既存VPN機器との関係

本プロジェクトは、既存の閉じたVPNルータ内部へ直接新しい制御機能を追加するものではない。

例えば、IX2215などのVPNルータが既に設置されている場合、その装置の内部IPsec制御や経路選択機能を、外部PCから自由に変更できるとは限らない。

既存機器が次の条件を満たさない場合、その装置単体に新しい経路選択機能を追加することは難しい。

外部APIが存在する

複数トンネルを柔軟に作成できる

経路制御を外部から変更できる

Policy Routingなどに対応している

状態を外部へ通知できる

そこで本プロジェクトでは、StrongSwan、VPP、FRRoutingなどを動作させるLinux PCまたはサーバを、独立したソフトウェアVPNゲートウェイとして配置する。

例えば次のような構成になる。

拠点LAN
   │
   ▼
OSS VPN Gateway
- Controller
- StrongSwan
- VPP
- FRRouting
   │
   ▼
既存ルータ
例: IX2215
   │
   ▼
Internet

既存ルータは、インターネット接続やNAT、通常のルーティングを担当する。

OSS VPN Gatewayは、拠点間VPN通信について次を担当する。

IPsec暗号化・復号

複数トンネルの確立

経路選択

転送制御

状態監視

切替制御

すべての通信をOSS VPN Gatewayへ通す構成も可能だが、初期導入ではVPN対象通信だけを通す構成が現実的である。

通常のInternet通信
    └→ 既存ルータ

拠点間通信
    └→ OSS VPN Gateway

5. IPsecとの関係

本プロジェクトは、標準IPsecの範囲内で実現する。

IPsec自体を拡張したり、独自のESP形式を導入したりしない。

5.1 IPsecが担当するもの

IKEv2による鍵交換

IKE SAの管理

CHILD SAの確立

ESPによる暗号化

通信相手の認証

SAの再鍵交換

Hub向けトンネルの確立

Direct向けトンネルの確立

Relay向けトンネルの確立

複数SAの同時保持

5.2 IPsecの外側で担当するもの

どのトンネルを利用するか

どの経路を選択するか

いつ切り替えるか

どのような切替方式を使うか

フロー単位で経路を維持するか

一時停止を許容するか

障害時にどの経路へ戻すか

RTTや損失をどう評価するか

これらはController、VPP、FRRouting、Linuxのルーティング機能などが担当する。

したがって、本プロジェクトの設計は、

標準IPsecで複数の暗号化経路を作り、その外側の制御プレーンで経路選択と経路遷移を実現する

という構成になる。

6. 独自データプレーン機能を避ける方針

以下の機能は、初期設計および本来の目的から外れるため実装対象外とする。

HubとDirectへの同一パケットの複製

独自の共通シーケンス番号

異なるIPsec SAをまたぐパケット順序制御

受信側の独自並べ替えバッファ

独自オーバーレイヘッダ

独自の重複排除

完全な無損失切替

完全な順序保証

これらを実現するには、IPsecの上に新しいデータプレーンプロトコルを作る必要があり、既存OSSを統合する制御プレーンという本来の目的から離れる。

本プロジェクトでは、標準IPsecと既存転送機能の範囲で、実用的かつ安全な経路切替を目指す。

7. 経路モデル

当初はHub経由とDirect経路の二つを想定していたが、設計上はこれらを特別扱いしない。

すべての通信経路を、共通のPathとして扱う。

7.1 経路例

Path 1:
Site A → Hub → Site B

Path 2:
Site A → Site B

Path 3:
Site A → Relay C → Site B

Path 4:
Site A → Security Gateway → Site B

Path 5:
Site A → Cloud POP → Site B

Direct経路はWaypointを持たないPathである。

Hub経由もRelay経由も、単に一つ以上のWaypointを持つPathとして表現する。

7.2 基本データモデル

Node
- node_id
- role
- endpoints
- capabilities
- administrative_state

Path
- path_id
- source
- destination
- waypoints
- segments
- priority
- constraints
- administrative_state
- operational_state

Segment
- segment_id
- from_node
- to_node
- tunnel_id

Tunnel
- tunnel_id
- local_node
- remote_node
- protocol
- state
- health

Transition
- transition_id
- current_path
- target_path
- strategy
- state
- timeout
- rollback_path

7.3 Pathの例

path:
  id: path-direct
  source: site-a
  destination: site-b
  waypoints: []

path:
  id: path-via-hub
  source: site-a
  destination: site-b
  waypoints:
    - hub-1

path:
  id: path-via-relay-c
  source: site-a
  destination: site-b
  waypoints:
    - relay-c

7.4 SegmentとTunnel

Relay経由のPathは、複数のSegmentから構成される。

Site A → Relay C → Site B

なら、次の二つのトンネルを持つ。

Tunnel 1:
Site A ↔ Relay C

Tunnel 2:
Relay C ↔ Site B

Controllerは、各Tunnelが利用可能かだけでなく、Path全体がEnd-to-Endで利用可能かを判定する。

8. 任意経路の扱い

制御プレーンがネットワーク内のあらゆるノードを自由に組み合わせ、無制限に経路を生成する設計にはしない。

無制限な経路生成を許可すると、次の問題が生じる。

経路候補数の爆発

ループ

意図しない中継

セキュリティ境界違反

管理者が把握できない経路

IPsecトンネル数の過剰増加

検証不能な構成

障害時の原因究明の困難化

そのため、Controllerが自動選択できるのは、管理者または上位システムが事前に許可したPath集合だけとする。

candidate_paths:
  - path-direct
  - path-via-hub
  - path-via-relay-c

自動選択とは、

任意の経路を自由生成すること

ではなく、

事前に許可された候補Path集合から、条件に合う経路を一つ選ぶこと

と定義する。

9. Intent

Intentは、利用者または上位システムが要求する目標状態を宣言的に記述するものである。

利用者は、StrongSwanやVPPの具体的なコマンドを書くのではなく、次のような要求を記述する。

Site AからSite Bへ通信させる

特定のPathを使用する

許可済みPathの中から自動選択する

通信暗号化を必須とする

特定Waypointを必須とする

特定Waypointを禁止する

切替時に通信継続を優先する

一瞬の停止を許容して順序乱れを抑える

切替速度を優先する

障害時には指定Pathへ戻す

9.1 明示的経路指定

intent:
  id: intent-a-b

  traffic:
    source: site-a
    destination: site-b

  path_selection:
    mode: explicit
    path_id: path-via-relay-c

  transition:
    strategy: graceful
    max_pause_ms: 50

  fallback:
    enabled: true
    path_id: path-via-hub

9.2 優先順位による選択

path_selection:
  mode: priority

  candidates:
    - path-direct
    - path-via-relay-c
    - path-via-hub

先頭から順に、利用可能なPathを選ぶ。

9.3 条件による自動選択

path_selection:
  mode: evaluated

  candidates:
    - path-direct
    - path-via-relay-c
    - path-via-hub

  constraints:
    encryption: required
    max_rtt_ms: 100
    max_packet_loss_percent: 1.0
    forbidden_waypoints:
      - relay-d

  comparison_order:
    - packet_loss
    - latency
    - hop_count
    - administrative_priority
    - path_id

10. Path Selection Policy

複数の候補Pathが存在する場合、制約条件だけでは一意に決まらない。

そのため、経路選択は次の三段階で行う。

1. 候補Pathの取得
2. 制約条件による除外
3. 比較規則による順位付け

10.1 候補取得

利用者または管理者が許可したPath集合を取得する。

Candidate Set:
- path-direct
- path-via-hub
- path-via-relay-c

10.2 制約評価

次のような制約を満たさないPathを除外する。

暗号化必須

最大RTT

最大損失率

必須Waypoint

禁止Waypoint

最大Hop数

特定地域を通ること

特定地域を通らないこと

管理停止中でないこと

全Segmentが確立可能であること

セキュリティ機能を持つWaypointを通ること

10.3 比較規則

制約を満たすPathが複数残った場合、明示された比較順序を用いる。

例:

1. packet lossが小さい
2. RTTが小さい
3. hop countが少ない
4. 管理者priorityが高い
5. path_idの辞書順

最後にpath_idを使うことで、すべての値が同じ場合でも一意に決定できる。

10.4 重み付きスコア

将来的には、複数指標を重み付きスコアとして評価することも可能である。

Score =
 latency_weight × normalized_latency
+ loss_weight × normalized_loss
+ hop_weight × normalized_hop

ただし、初期実装では次の問題がある。

指標の正規化が必要

重みの根拠が曖昧になりやすい

利用者に選択理由を説明しにくい

測定値の揺らぎに弱い

そのため、初期実装では辞書式の優先比較を採用する。

11. 経路選択の安定化

経路が一意に決まっても、測定値のわずかな変化で頻繁に切り替えてはならない。

例えば、

Path A RTT: 20 ms
Path B RTT: 19 ms

次の測定で、

Path A RTT: 18 ms
Path B RTT: 20 ms

となるたびに経路を切り替えると、経路フラッピングが発生する。

そのため、次の安定化機構を導入する。

11.1 ヒステリシス

現在のPathより一定以上改善しなければ切り替えない。

minimum_improvement:
  latency_ms: 10
  packet_loss_percent: 0.2

11.2 連続成立回数

候補Pathが複数回連続で条件を満たした場合のみ切り替える。

confirmation_samples: 3

11.3 ホールドダウン

切替後、一定時間は再切替しない。

hold_down_ms: 30000

11.4 最低利用時間

現在Pathを最低限維持する時間を設定する。

minimum_active_time_ms: 60000

11.5 緊急障害時の例外

現在Pathが完全に利用不能となった場合は、安定化条件を無視して即時フェイルオーバーする。

12. 経路選択モード

初期仕様では、次の三つの選択モードを定義する。

12.1 Explicit

経路を直接指定する。

path_selection:
  mode: explicit
  path_id: path-via-relay-c

用途:

実験

検証

管理者による強制指定

特定経由地の利用

12.2 Priority

優先順位リストの先頭から、利用可能なPathを選ぶ。

path_selection:
  mode: priority
  candidates:
    - path-direct
    - path-via-relay-c
    - path-via-hub

用途:

単純なフェイルオーバー

Primary/Secondary構成

管理者が明確な順位を持つ場合

12.3 Evaluated

制約と比較規則に基づいて選択する。

path_selection:
  mode: evaluated

  candidates:
    - path-direct
    - path-via-relay-c
    - path-via-hub

  constraints:
    max_rtt_ms: 100
    max_packet_loss_percent: 1

  comparison_order:
    - packet_loss
    - latency
    - hop_count
    - administrative_priority
    - path_id

用途:

品質に応じた経路選択

実験的アルゴリズムの評価

動的ネットワーク制御

初期実装では、ExplicitとPriorityを先に完成させ、Evaluatedを次段階で実装する。

13. Path Transition Policy

本プロジェクトでは、経路を「どこへ変更するか」だけでなく、「どのように変更するか」もIntentとして指定できる。

これをPath Transition Policyと呼ぶ。

経路選択と経路遷移を分ける。

Path Selection:
どのPathを使うか

Path Transition:
現在Pathから新Pathへどう移るか

初期仕様では次の三方式を用意する。

14. Immediate Transition

現在Pathから候補Pathへ即時に切り替える。

Current Path
    ↓
Forwarding変更
    ↓
Target Path

特徴

切替が速い

実装が単純

短時間のパケット損失が起こり得る

パケット順序入れ替わりが起こり得る

TCP再送が発生する可能性がある

UDPパケットが失われる可能性がある

用途

障害時の緊急切替

短時間の停止を許容できる通信

切替速度を優先する場合

15. Graceful Transition

旧経路への新規パケット投入を停止し、一定時間待ってから新経路へ切り替える。

旧Pathへの投入停止
        ↓
短時間ドレイン
        ↓
Forwarding変更
        ↓
新Pathを利用

特徴

順序入れ替わりを抑えられる

短時間の通信停止が発生する

完全な無損失は保証しない

既存IPsecの範囲内で実現できる

用途

数十ms程度の停止を許容できる通信

順序乱れを抑えたい通信

計画的な経路変更

設定例

transition:
  strategy: graceful
  max_pause_ms: 50
  drain_timeout_ms: 40

16. Flow Preserve Transition

既存フローは旧Pathに残し、新規フローだけを新Pathへ割り当てる。

既存フロー
    └→ Current Path

新規フロー
    └→ Target Path

フロー識別

原則として次の5-Tupleを用いる。

送信元IP

宛先IP

送信元ポート

宛先ポート

プロトコル

特徴

既存TCPセッションを維持しやすい

同一フロー内の順序入れ替わりを抑えられる

一時停止を避けられる

旧Pathを長時間維持する必要がある

長時間接続が残る可能性がある

フロー状態管理が必要

長時間フロー

次の通信は長時間残る可能性がある。

SSH

WebSocket

動画配信

VPN内VPN

長時間TCPセッション

対応方針はPolicyで選択できるようにする。

transition:
  strategy: flow-preserve
  existing_flow_timeout_ms: 3600000
  force_migrate_after_timeout: false

初期実装では、既存フローは旧Pathに残し、タイムアウト後に旧Pathを削除できる構成を目指す。

17. パケット順序入れ替わり

経路切替時には、旧Pathを通っているパケットより、新Pathを通る後続パケットが先に到着する可能性がある。

例:

Packet 100 → Hub経由
経路切替
Packet 101 → Direct経由

Direct経路の遅延が小さい場合、次の順で到着する可能性がある。

101
100

17.1 TCP

TCPは一定範囲の順序入れ替わりを受信バッファで処理できる。

ただし、大きな順序乱れが起こると次の影響がある。

Duplicate ACK

Fast Retransmit

不要な再送

輻輳ウィンドウ縮小

一時的な性能低下

17.2 UDP

UDPには共通の順序制御がない。

順序制御や遅延パケット処理はアプリケーションに依存する。

17.3 対策

本プロジェクトでは次の対策を採る。

通信継続を優先する場合はFlow Preserve

順序安定性を優先する場合はGraceful

緊急時はImmediate

独自の並べ替えプロトコルは作らない

完全順序保証は目標としない

18. Make Before Break

新Pathへ切り替える前に、候補Pathを事前に準備する。

Current Path Active

        ↓

Target Tunnel Establishing

        ↓

Target Path Validating

        ↓

Target Path Ready

        ↓

Transition

        ↓

Target Path Active

        ↓

Old Path Standby / Removal

重要なのは、二つのPathが同時に存在することと、同じパケットを二重送信することは別である。

Make Before Breakでは、

Current Path: Active
Target Path: Ready

という状態を作る。

切替後は、

Current Path: Standby
Target Path: Active

とする。

同じ通信を両方へ同時に送らない。

19. 状態モデル

Controllerは、最低限次の状態を管理する。

Desired State

Observed State

Applied State

Transition State

Health State

Error State

19.1 Desired State

利用者がIntentで要求した状態。

例:

Target Path: path-direct
Transition Strategy: graceful
Fallback Path: path-via-hub

19.2 Observed State

各OSSや監視機構から実際に観測された状態。

例:

Hub Tunnel: ESTABLISHED
Direct Tunnel: ESTABLISHED
Active Path: HUB
Direct RTT: 25 ms

19.3 Applied State

Controllerが各OSSへ適用したと認識している状態。

Observed StateとApplied Stateは必ずしも一致しない。

例:

Applied:
Forwarding to Direct

Observed:
Traffic still using Hub

この差異を検出し、再適用またはエラー処理を行う。

19.4 Transition State

経路遷移の進行状態。

IDLE
PREPARING
VALIDATING
READY
DRAINING
SWITCHING
VERIFYING
COMPLETED
ROLLING_BACK
FAILED

19.5 Health State

Path、Tunnel、Segment、Nodeの健全性。

UNKNOWN
HEALTHY
DEGRADED
UNHEALTHY
FAILED

19.6 Error State

処理失敗の種類を保持する。

例:

TUNNEL_ESTABLISH_TIMEOUT

PATH_VALIDATION_FAILED

FORWARDING_UPDATE_FAILED

STATE_CONFLICT

ROLLBACK_FAILED

OBSERVATION_TIMEOUT

CONTROLLER_RESTART_RECOVERY_REQUIRED

20. Tunnel State

StrongSwanから観測するTunnel状態を次のように定義する。

ABSENT
CONFIGURED
ESTABLISHING
ESTABLISHED
REKEYING
DEGRADED
FAILED
DELETING
UNKNOWN

Tunnelが確立していることと、Pathが利用中であることは分離する。

例:

Hub Tunnel: ESTABLISHED
Direct Tunnel: ESTABLISHED
Active Path: Hub

この分離がなければ、Make Before Breakを表現できない。

21. Path State

Path状態は次のように定義する。

UNAVAILABLE
PREPARING
READY
ACTIVE
STANDBY
DEGRADED
FAILED
DRAINING
UNKNOWN

状態の意味

UNAVAILABLE

必要なTunnelやSegmentが存在せず、利用できない。

PREPARING

必要なTunnel、Forwarding、Routingなどを準備している。

READY

End-to-End検証が成功し、いつでも切替可能。

ACTIVE

現在の転送経路として利用中。

STANDBY

利用可能だが、現在は転送に使っていない。

DEGRADED

通信可能だが、品質や一部機能に問題がある。

FAILED

通信経路として利用不能。

DRAINING

既存通信の終了または旧パケットの排出を待っている。

22. 経路遷移状態機械

一般化した状態遷移は次のとおりである。

IDLE
  ↓
CANDIDATE_SELECTED
  ↓
PREPARING
  ↓
VALIDATING
  ↓
READY
  ↓
TRANSITIONING
  ↓
VERIFYING
  ↓
COMPLETED

失敗した場合:

PREPARING
  ↓
FAILED
  ↓
ROLLING_BACK
  ↓
IDLE

または、

VERIFYING
  ↓
FAILED
  ↓
ROLLING_BACK
  ↓
CURRENT_PATH_ACTIVE

22.1 Immediateの場合

READY
  ↓
SWITCHING
  ↓
VERIFYING
  ↓
COMPLETED

22.2 Gracefulの場合

READY
  ↓
PAUSING
  ↓
DRAINING
  ↓
SWITCHING
  ↓
VERIFYING
  ↓
COMPLETED

22.3 Flow Preserveの場合

READY
  ↓
STOP_NEW_FLOW_ASSIGNMENT_TO_OLD_PATH
  ↓
ASSIGN_NEW_FLOWS_TO_TARGET
  ↓
WAIT_EXISTING_FLOWS
  ↓
OLD_PATH_STANDBY_OR_REMOVED

23. 両端の同期

Path切替では、通信の両端が異なる状態になる可能性がある。

例:

Site A:
Directへ切替済み

Site B:
まだHubを利用

この状態では、非対称経路やブラックホールが発生する可能性がある。

そのため、両端の切替は次のようなプロトコルで進める。

Prepare
   ↓
Ready
   ↓
Commit
   ↓
Confirm

Prepare

各拠点が候補Pathに必要なTunnel、Routing、Forwardingを準備する。

Ready

各拠点が候補Pathを使用可能であることをControllerへ通知する。

Commit

Controllerが切替を指示する。

Confirm

各拠点が切替後の状態を報告する。

厳密な分散トランザクションを実装するのではなく、タイムアウトとロールバックを備えた状態機械として実装する。

24. ロールバック

新Pathへの切替に失敗した場合、旧Pathへ戻せる必要がある。

ロールバック条件の例:

候補Tunnelの確立失敗

End-to-End疎通確認失敗

Forwarding更新失敗

切替後の通信断

RTTや損失が許容値を超えた

対向拠点からConfirmが届かない

状態不整合

Controller再起動

Adapterエラー

ロールバック時には、可能な限り旧Pathを削除せず、Standbyとして維持する。

Target Path切替失敗
        ↓
旧Pathを再Active化
        ↓
Target PathをStandbyまたはFailedへ
        ↓
エラー記録

25. State Synchronization Engine

State Synchronization Engineは、本プロジェクトの中核である。

各OSSは異なる状態モデルを持つ。

StrongSwan:

IKE SA

CHILD SA

Peer

Rekey

DPD

VPP:

Interface

FIB

Route

Adjacency

Session

Counter

FRRouting:

BGP

OSPF

Route

Next Hop

BFD

Controllerはこれらを共通状態へ変換する。

OSS固有状態
    ↓
Adapter Normalization
    ↓
Unified State Model

State Synchronization Engineは次を担当する。

OSSイベントの受信

状態の正規化

Desired Stateとの差分計算

Applied Stateの追跡

Observed Stateの更新

不整合検出

再適用

タイムアウト

エラー状態への遷移

再起動後の復旧

25.1 研究課題

状態同期には未解決部分が多い。

イベント順序の逆転

イベント欠落

同時更新

ControllerとOSSの状態競合

部分適用

Adapter停止

Controller再起動

一時的な不整合

最終的整合性

ロールバックの正確性

初期実装ではすべてを完全解決するのではなく、モデルを提示し、主要部分を実装し、限界と課題を明示する。

26. イベントモデル

Controllerはイベント駆動で動作する。

26.1 Intentイベント

INTENT_CREATED

INTENT_UPDATED

INTENT_DELETED

MANUAL_PATH_SWITCH_REQUESTED

26.2 StrongSwanイベント

IKE_SA_ESTABLISHING

IKE_SA_ESTABLISHED

IKE_SA_DELETED

CHILD_SA_ESTABLISHED

CHILD_SA_REKEYED

CHILD_SA_FAILED

DPD_TIMEOUT

PEER_UNREACHABLE

26.3 VPPイベント

INTERFACE_UP

INTERFACE_DOWN

ROUTE_INSTALLED

ROUTE_REMOVED

FORWARDING_UPDATE_FAILED

FLOW_CREATED

FLOW_EXPIRED

COUNTER_THRESHOLD_EXCEEDED

26.4 FRRoutingイベント

ROUTE_ADDED

ROUTE_REMOVED

BGP_NEIGHBOR_UP

BGP_NEIGHBOR_DOWN

BFD_SESSION_DOWN

NEXT_HOP_CHANGED

26.5 監視イベント

PATH_HEALTHY

PATH_DEGRADED

PATH_FAILED

RTT_THRESHOLD_EXCEEDED

LOSS_THRESHOLD_EXCEEDED

JITTER_THRESHOLD_EXCEEDED

VALIDATION_SUCCESS

VALIDATION_FAILED

26.6 Controller内部イベント

TRANSITION_STARTED

TRANSITION_TIMEOUT

ROLLBACK_STARTED

ROLLBACK_COMPLETED

STATE_CONFLICT_DETECTED

RECONCILIATION_REQUIRED

27. Health Monitoring

初期実装で扱う指標は次のとおりである。

Reachability

RTT

Packet Loss

Jitter

Tunnel State

Interface State

Last Updated

Consecutive Failures

Consecutive Successes

27.1 Path Health

Path全体のHealthは、各SegmentおよびEnd-to-End監視から算出する。

Path Healthy:
すべての必須SegmentがHealthy
かつ
End-to-End疎通成功

27.2 一時的な測定変動

単発の失敗ですぐにPathをFailedにしない。

health_policy:
  unhealthy_after_failures: 3
  healthy_after_successes: 3
  observation_interval_ms: 1000

28. 各コンポーネントの責務

28.1 Intent API

Intent受付

構文検証

スキーマ検証

認可

更新履歴管理

28.2 Policy Engine

Intent解釈

制約評価

Path候補の取得

遷移方式の決定

Fallback条件の決定

28.3 Path Selection Engine

候補Pathの絞り込み

Path比較

タイブレーク

ヒステリシス

ホールドダウン

最終候補決定

28.4 Transition Engine

Path準備

Path検証

両端同期

Immediate切替

Graceful切替

Flow Preserve切替

切替後検証

ロールバック

28.5 State Synchronization Engine

OSS状態の統合

状態差分の検出

Reconciliation

エラー検出

再起動復旧

28.6 StrongSwan Adapter

Peer設定

IKE SA作成

CHILD SA作成

SA削除

VICIイベント購読

SA状態取得

DPD状態取得

28.7 VPP Adapter

インターフェース管理

FIB更新

経路切替

Policy Routing

Flow分類

Flow Preserve制御

Counter取得

転送状態取得

28.8 FRRouting Adapter

動的ルーティング

BGP/OSPF制御

Route監視

BFD連携

Next Hop変更

ただし、FRRoutingは初期実装では必須としない。

28.9 Health Probe

End-to-End疎通確認

RTT測定

Packet Loss測定

Jitter測定

Path Validation

28.10 Event Store / Audit Log

Intent変更履歴

状態変化

Path選択理由

切替履歴

障害履歴

ロールバック履歴

Adapter操作履歴

29. 初期実装におけるFRRoutingの扱い

初期実装では、StrongSwanとVPPを中心とする。

Intent/API
    │
Controller
    │
    ├── StrongSwan Adapter
    ├── VPP Adapter
    └── Health Probe

Hub、Direct、Relayの明示的Path切替だけであれば、VPPのFIBや転送制御で実現できる可能性が高い。

最初からFRRoutingまで統合すると、研究テーマが三つのOSSの統合作業に埋もれる可能性がある。

そのため次の段階構成とする。

第1段階

StrongSwan

VPP

Controller

Health Probe

Explicit Path Selection

Priority Path Selection

Immediate Transition

Graceful Transition

ロールバック

第2段階

Flow Preserve

Evaluated Path Selection

自動フェイルオーバー

安定化制御

状態同期の強化

第3段階

FRRouting Adapter

BGP/OSPF連携

BFD連携

多拠点

複数Waypoint

サービスチェイン

30. 初期実証トポロジ

最初の実証は、2拠点、1 Hub、1 Relayとする。

             Hub
            /   \
           /     \
       Site A   Site B

Site A ───────── Site B
       Direct

Site A ─ Relay C ─ Site B

候補Pathは次の三つである。

Path 1:
Site A → Hub → Site B

Path 2:
Site A → Site B

Path 3:
Site A → Relay C → Site B

この構成により、次を検証できる。

Hub経由からDirectへの切替

DirectからHubへの復帰

Hub経由からRelay経由への切替

Relay障害時のFallback

Explicit選択

Priority選択

条件選択

Transition Policyの比較

31. 初期実証シナリオ

S1. 明示的Path切替

管理者がIntentでPathを指定し、現在Pathから指定Pathへ切り替える。

S2. 候補Pathの事前確立

現在Pathを維持したまま、候補Pathに必要なIPsecトンネルを確立する。

S3. Path Validation

候補PathへのEnd-to-End疎通を確認する。

S4. Immediate切替

即時にForwardingを変更し、切替時間、損失、順序変化を測定する。

S5. Graceful切替

短時間のドレインを挟み、停止時間、損失、順序変化を測定する。

S6. Flow Preserve切替

既存フローを旧Pathに残し、新規フローを新Pathへ割り当てる。

S7. 候補Path確立失敗

候補Tunnelが確立できない場合に、現在Pathを維持する。

S8. 切替後検証失敗

切替後に疎通できない場合、旧Pathへロールバックする。

S9. Direct障害時のHub復帰

Direct Path障害を検出し、Hub Pathへ戻す。

S10. Relay障害時の別Path選択

Relay Pathが利用不能となった場合、候補集合から別Pathを選択する。

S11. 優先順位選択

Priorityリストに従い、利用可能な最上位Pathを選択する。

S12. 条件選択

RTT、損失、Hop数などの比較規則により、Pathを一意に選択する。

32. 評価項目

32.1 制御プレーン性能

Intent受付から処理開始までの時間

Policy評価時間

Path選択時間

状態反映時間

Reconciliation時間

32.2 Tunnel処理

IKE SA確立時間

CHILD SA確立時間

Tunnel再確立時間

Rekey時の影響

32.3 Path遷移

Path準備時間

Validation時間

Forwarding切替時間

全体の遷移時間

通信停止時間

Packet Loss

Packet Reordering

TCP再送数

UDP損失数

32.4 障害復旧

障害検出時間

Fallback開始時間

Fallback完了時間

ブラックホール時間

ロールバック成功率

32.5 リソース

CPU使用率

メモリ使用量

VPP処理量

IPsecスループット

同時Tunnel数

同時Flow数

32.6 状態同期

イベント反映遅延

状態不整合検出時間

再同期成功率

Controller再起動後の復旧時間

Adapter切断後の回復

33. 成功条件の考え方

本プロジェクトは、完全なゼロロスや完全な瞬断ゼロを必須条件としない。

優先順位は次のとおりとする。

長時間のブラックホールを発生させない

切替失敗時に旧Pathへ戻れる

切替状態を観測・説明できる

複数OSSの状態を統一的に管理できる

Intentに基づいてPathを制御できる

Transition Policyを選択できる

通信停止と順序乱れを最小化する

将来的な拡張が可能である

34. Explain機能

Explain機能は、本プロジェクトの主要な新規性ではない。

ただし、研究成果の説明、デモ、利用者理解には重要である。

Explain機能では次を表示する。

選択Path:
path-via-relay-c

候補:
path-direct
path-via-hub
path-via-relay-c

除外理由:
path-direct:
packet loss constraint violation

path-via-hub:
higher RTT

選択理由:
path-via-relay-c had the lowest packet loss,
then the lowest RTT among valid paths.

Transition:
graceful

Reason:
application policy allows up to 50 ms pause
and prefers reduced packet reordering.

Explainは次を記録する。

候補Path

除外されたPath

制約違反

比較結果

タイブレーク

Transition Strategy

Fallback理由

ロールバック理由

35. 新規性

本プロジェクトの新規性は、次の個別技術そのものではない。

IPsec

StrongSwan

VPP

FRRouting

Hub-to-Direct

SLAベース経路選択

フェイルオーバー

Policy Routing

新規性の中心は、次の統合モデルにある。

35.1 複数OSSの統一状態モデル

異なるOSSの状態を、Desired、Observed、Applied、Transitionとして統一的に扱う。

35.2 イベント駆動制御

定期的な設定投入だけでなく、OSSイベント、品質変化、Intent更新に反応して状態を収束させる。

35.3 PathとTransitionの分離

経路選択だけでなく、経路遷移方式までPolicyとして扱う。

Target Path:
path-direct

Transition:
flow-preserve

35.4 汎用Pathモデル

HubとDirectを特別扱いせず、Direct、Hub、Relay、Security Gatewayなどを共通Pathとして扱う。

35.5 研究可能な制御基盤

商用機器では変更できない制御アルゴリズムや状態遷移を、OSS上で実装・比較できる。

36. 非目標

初期段階では次を目標としない。

商用VPNルータの完全代替

Cisco、Fortinet、Palo Altoなどとの機能競争

独自VPNプロトコル

独自暗号方式

完全無停止

完全無損失

完全順序保証

大規模商用運用

多数ベンダー機器の統一管理

AIによる自律ネットワーク

完成度の高いGUI

Zero Trust全機能

SASE全機能

任意トポロジの無制限自動生成

37. 将来拡張

将来的にはAdapter方式により、次のOSSを追加できる設計とする。

VPN

WireGuard

OpenVPN

IPsec実装の追加

Routing

BIRD

GoBGP

Linux Routing

FRRouting

Forwarding

Linux Kernel

eBPF/XDP

OVS

DPDK

SmartNIC

Monitoring

Prometheus

IPFIX

sFlow

eBPF Telemetry

制御対象

Zero Trust Gateway

Service Chaining

Edge Network

Cloud Interconnect

Multi-Cloud

SASE

セキュリティ経由制御

地理制約経路

38. 現時点での初期仕様

初期実装では、次を正式な範囲とする。

トポロジ

Site A

Site B

Hub

Relay C

Path

Site A → Hub → Site B

Site A → Site B

Site A → Relay C → Site B

使用OSS

StrongSwan

VPP

Controller

Health Probe

FRRoutingは後段とする。

Path Selection

Explicit

Priority

Evaluatedの基本形

Transition

Immediate

Graceful

Flow Preserveは段階的実装

状態管理

Desired State

Observed State

Applied State

Transition State

Health State

Error State

制御機能

Tunnel準備

Path検証

Forwarding切替

ロールバック

障害検出

Fallback

イベント記録

Explain

39. 最終的なプロジェクト定義

本プロジェクトは、StrongSwanやVPPなどの既存ネットワークOSSをデータプレーンとして利用し、それらをIntent、統一状態モデル、イベント駆動制御、Path Selection Policy、Path Transition Policyによって統合する、オープンなネットワーク制御基盤を構築するものである。

ネットワーク経路は、Hub、Direct、Relayなどの固定的な分類ではなく、複数のTunnelとWaypointから構成されるPathとして表現する。

制御プレーンは、管理者が許可した候補Path集合から、制約、優先順位、品質情報、安定化条件に基づいて一つのPathを選択する。

経路変更時には、Immediate、Graceful、Flow PreserveなどのTransition Policyを利用者が選択できる。

これにより、

切替速度を優先する

通信継続を優先する

順序乱れを抑える

特定経由地を利用する

障害時に別経路へ戻す

といった要求を、データプレーンを独自拡張せずに実現する。

本プロジェクトの最終的な価値は、特定のVPN製品を作ることではなく、ネットワーク制御方式を自由に実装、比較、検証できる、オープンで拡張可能な研究・実証基盤を提供することにある。